# This is auto generated. Do not edit this manually

---
- name: po300
  type: pc
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: trunk

- name: po301
  type: pc
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: access

- name: po302
  type: pc
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: l3

- name: po303
  type: pc
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: monitor

- name: "{{ test_data_common.eth_intf2 }}"
  type: eth
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: trunk

- name: "{{ test_data_common.eth_intf3 }}"
  type: eth
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: access

- name: "{{ test_data_common.eth_intf5 }}"
  type: eth
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: epl_routed
    ipv4_addr: 192.168.1.1

- name: "{{ test_data_common.eth_intf6 }}"
  type: eth
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: monitor

- name: "{{ test_data_common.sub_intf1 }}"
  type: sub_int
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: subint
    vlan: 100
    ipv4_addr: 193.168.2.1
    ipv4_mask_len: 24

- name: lo100
  type: lo
  switch:
    - "{{ test_data_common.switch1 }}"
  deploy: {{ test_data_common.deploy | bool }}
  profile:
    mode: lo
    ipv4_addr: 194.168.3.1
