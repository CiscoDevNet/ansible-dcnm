##############################################
##               SETUP                      ##
##############################################

- name: Remove local log file
  local_action: command rm -f dcnm_sgrp_association.log

- name: Put the fabric to default state
  cisco.dcnm.dcnm_sgrp_association:
    fabric: "{{ ansible_it_fabric }}"
    state: deleted                      # choose from [merged, replaced, deleted, overridden, query]
    deploy: switches                    # choose from ["none", "switches"]
  register: result

- assert:
    that:
      - 'item["RETURN_CODE"] == 200'
  loop: '{{ result.response }}'


- block:

##############################################
##                MERGE                     ##
##############################################

    - name: Create Security Group Associations - with and without mentioning group IDs
      cisco.dcnm.dcnm_sgrp_association: &sgrp_assoc_create
        fabric: "{{ ansible_it_fabric }}"
        deploy: switches                                    # choose from ["none", "switches"]
        state: merged                                       # choose from [merged, replaced, deleted, overridden, query]
        config:
          - src_group_name: "LSG_15001"
            dst_group_name: "LSG_15001"
            src_group_id: 15001                             # Group Id associated with src_group_name
            dst_group_id: 15001                             # Group Id associated with dst_group_name
            vrf_name: "MyVRF_50001"
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"

          - src_group_name: "LSG_15002"
            dst_group_name: "LSG_15002"
            vrf_name: "MyVRF_50002"
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result

    - assert:
        that:
          - 'result.changed == true'
          - '(result["diff"][0]["merged"] | length) == 2'
          - '(result["diff"][0]["deleted"] | length) == 0'
          - '(result["diff"][0]["query"] | length) == 0'
          - '(result["diff"][0]["deploy"] | length) == 1'

##############################################
##              IDEMPOTENCE                 ##
##############################################

    - name: Create Security Group Associations - Idempotence
      cisco.dcnm.dcnm_sgrp_association: *sgrp_assoc_create
      register: result

    - assert:
        that:
          - 'result.changed == false'
          - '(result["diff"][0]["merged"] | length) == 0'
          - '(result["diff"][0]["deleted"] | length) == 0'
          - '(result["diff"][0]["query"] | length) == 0'
          - '(result["diff"][0]["deploy"] | length) == 0'

    - name: Create Security Group Association without mentioning state
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        deploy: switches                                    # choose from ["none", "switches"]
        config:
          - src_group_name: "LSG_15001"
            dst_group_name: "LSG_15002"
            src_group_id: 15001                             # Group Id associated with src_group_name
            dst_group_id: 15002                             # Group Id associated with dst_group_name
            vrf_name: "MyVRF_50001"
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result

    - assert:
        that:
          - 'result.changed == true'
          - '(result["diff"][0]["merged"] | length) == 1'
          - '(result["diff"][0]["deleted"] | length) == 0'
          - '(result["diff"][0]["query"] | length) == 0'
          - '(result["diff"][0]["deploy"] | length) == 1'

    - name: Create Security Group Association - without deploy flag
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        state: merged                                       # choose from [merged, replaced, deleted, overridden, query]
        config:
          - src_group_name: "LSG_15003"
            dst_group_name: "LSG_15004"
            src_group_id: 15003                             # Group Id associated with src_group_name
            dst_group_id: 15004                             # Group Id associated with dst_group_name
            vrf_name: "MyVRF_50001"
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result

    - assert:
        that:
          - 'result.changed == true'
          - '(result["diff"][0]["merged"] | length) == 1'
          - '(result["diff"][0]["modified"] | length) == 0'
          - '(result["diff"][0]["deleted"] | length) == 0'
          - '(result["diff"][0]["query"] | length) == 0'
          - '(result["diff"][0]["deploy"] | length) == 1'

    - name: Create Security Group Association - without src_group_name
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        state: merged                                       # choose from [merged, replaced, deleted, overridden, query]
        config:
          - dst_group_name: "LSG_15004"
            src_group_id: 15003                             # Group Id associated with src_group_name
            dst_group_id: 15004                             # Group Id associated with dst_group_name
            vrf_name: "MyVRF_50001"
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result
      ignore_errors: yes

    - assert:
        that:
          - 'result.changed == false'
          - '("Invalid parameters in playbook" in result["msg"])'
          - "('src_group_name : Required parameter not found' in result['msg'])"

    - name: Create Security Group Association - without dst_group_name
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        state: merged                                       # choose from [merged, replaced, deleted, overridden, query]
        config:
          - src_group_name: "LSG_15003"
            src_group_id: 15003                             # Group Id associated with src_group_name
            st_group_id: 15004                             # Group Id associated with dst_group_name
            vrf_name: "MyVRF_50001"
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result
      ignore_errors: yes

    - assert:
        that:
          - 'result.changed == false'
          - '("Invalid parameters in playbook" in result["msg"])'
          - "('dst_group_name : Required parameter not found' in result['msg'])"

    - name: Create Security Group Association - without vrf_name
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        state: merged                                       # choose from [merged, replaced, deleted, overridden, query]
        config:
          - src_group_name: "LSG_15003"
            dst_group_name: "LSG_15003"
            src_group_id: 15003                             # Group Id associated with src_group_name
            dst_group_id: 15004                             # Group Id associated with dst_group_name
            contract_name: CONTRACT1
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result
      ignore_errors: yes

    - assert:
        that:
          - 'result.changed == false'
          - '("Invalid parameters in playbook" in result["msg"])'
          - "('vrf_name : Required parameter not found' in result['msg'])"

    - name: Create Security Group Association - without contract_name
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        state: merged                                       # choose from [merged, replaced, deleted, overridden, query]
        config:
          - src_group_name: "LSG_15003"
            dst_group_name: "LSG_15004"
            src_group_id: 15003                             # Group Id associated with src_group_name
            dst_group_id: 15004                             # Group Id associated with dst_group_name
            vrf_name: "MyVRF_50001"
            switch:
              - "{{ ansible_switch1 }}"
              - "{{ ansible_switch2 }}"
      register: result
      ignore_errors: yes

    - assert:
        that:
          - 'result.changed == false'
          - '("Invalid parameters in playbook" in result["msg"])'
          - "('contract_name : Required parameter not found' in result['msg'])"

##############################################
##                CLEANUP                   ##
##############################################

  always:

    - name: Delete all created security group associations
      cisco.dcnm.dcnm_sgrp_association:
        fabric: "{{ ansible_it_fabric }}"
        state: deleted                     # choose from [merged, replaced, deleted, overridden, query]
        deploy: switches                   # choose from ["none", "switches"]
      register: result
      when: IT_CONTEXT is not defined

    - assert:
        that:
          - 'item["RETURN_CODE"] == 200'
          - 'item["MESSAGE"] == "OK"'
      loop: '{{ result.response }}'
      when: IT_CONTEXT is not defined
