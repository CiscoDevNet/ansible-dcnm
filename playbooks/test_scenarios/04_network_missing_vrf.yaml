---
# Baseline seed for scenario 04.
# Existing state uses a valid, existing VRF reference.

- hosts: ndfc
  gather_facts: false
  connection: ansible.netcommon.httpapi

  vars:
    fabric_name: cisco_test_fabric1
    leaf1: 192.168.10.211

  tasks:
    - name: Ensure PROD_NET_Orphan exists on existing VRF (baseline)
      cisco.dcnm.dcnm_network:
        fabric: "{{ fabric_name }}"
        state: merged
        config:
          - net_name: PROD_NET_Orphan
            vrf_name: PROD_VRF_Core
            net_id: 30400
            net_template: Default_Network_Universal
            net_extension_template: Default_Network_Extension_Universal
            vlan_id: 3040
            gw_ip_subnet: "10.104.1.1/24"
            attach:
              - ip_address: "{{ leaf1 }}"
                ports: []
